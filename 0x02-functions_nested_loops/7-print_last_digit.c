#include "main.h"
/**
 * print_last_digit - prints the last digit
 * of integer numbers
 *
 * @n: input number as integer
 *
 * Return: last digit
 */
int print_last_digit(int n)
{
	int m;

	m = n % 10;
	if (n < 0)
	{
		_putchar(-l + 48);
		return (-l);
	}
	else
	{
		_putchar(l + 48);
		return (l);
	}
}
