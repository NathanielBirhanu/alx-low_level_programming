#include "main.h"
/**
 * mul - multiplies two integers a and b
 * and returns the product
 * @a: integer parameter multiplied to b
 * @b: integer parameter multiplied to a
 * Return: the product of two integers
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
